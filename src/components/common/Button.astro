---
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
  class?: string;
  i18n?: string;
}

const { href, variant = 'primary', class: className = '', i18n } = Astro.props;

const variants = {
  primary:
    'bg-brand text-paper hover:bg-ink',
  secondary:
    'border border-brand/25 text-ink hover:border-brand hover:text-brand',
};
---

<a
  href={href}
  class:list={[
    'inline-flex items-center justify-center gap-2 rounded-full px-6 py-3 text-sm font-semibold tracking-wide transition-all duration-300',
    variants[variant],
    className,
  ]}
  data-i18n={i18n}
>
  <slot />
</a>
